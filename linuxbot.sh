#!/bin/bash

# Run the application (change path as needed)
cd ~/_DEV/_INTERNAL/LinuxBot/app/
python3 app.py

# INSTRUCTIONS: Move linuxbot.sh to /usr/local/bin/ and make it executable
# with the combined command: sudo cp linuxbot.sh /usr/local/bin/linuxbot.sh && sudo chmod +x /usr/local/bin/linuxbot.sh
